Made Redis-backed Bloom filters fail quickly when Redis becomes unavailable without changing Pulpcore's Redis retry policy. Filters now record their repository version so stale data cannot reject new content after Redis recovers, and can catch up from an older retained repository version. Error messages for configuration, lookup, update, and cleanup failures now explain the fallback and recovery steps.
